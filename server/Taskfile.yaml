version: "3"

tasks:
  image:
    cmds:
      - docker build -t {{.REGISTRY}}/vge.server:latest .
  publish:
    cmds:
      - docker push {{.REGISTRY}}/vge.server:latest
  ci:format:
    cmds:
      - gradle spotlessApply
